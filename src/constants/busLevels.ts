import { MIN_DBFS_VALUE } from "./audioLevels";

// Meter
export const DBFS_NOMINAL = -12;

// Fader and Buss
export const BUS_MAX_GAIN = 12;
export const BUS_MIN_GAIN = MIN_DBFS_VALUE;

// Pad
export const DEFAULT_PAD_LEVEL = -20;

// Gain Knob
export const PRE_AMP_MIN_GAIN = 10;
export const PRE_AMP_MAX_GAIN = 50;