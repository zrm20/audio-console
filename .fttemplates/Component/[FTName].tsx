import React from 'react';

import useStyles from './[FTName].styles';

interface <FTName | capitalize>Props {

};

export default function <FTName | capitalize>(props: <FTName | capitalize>Props): JSX.Element {
  const styles = useStyles();

  return (
    <div>

    </div>
  );
};