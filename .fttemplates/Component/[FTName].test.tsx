import { render, screen } from '@testing-library/react';

import <FTName | capitalize> from './[FTName]';

describe('<[FTName] />', () => {
  it("should render", () => {
    render(<<FTName> />);
  });

});